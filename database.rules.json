{
  "rules": {
    "sessions": {
      "$sessionId": {
        ".read": true,
        ".write": true,
        ".validate": "$sessionId.matches(/^[A-Z0-9]{4,10}$/)"
      }
    },
    "samples": {
      "$sessionId": {
        ".read": true,
        ".write": true,
        ".validate": "$sessionId.matches(/^[A-Z0-9]{4,10}$/)"
      }
    }
  }
}
